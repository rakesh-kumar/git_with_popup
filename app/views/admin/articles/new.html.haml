- use_kindeditor
%h2#title=w('new.article')
#action-links
  %ul
    %li.no-icon
      -if blog.id
        =link_to 'Cancel',blog_path(blog)
      -else
        =link_to 'Cancel',pages_path
=render partial:"shared/noscript"
=render partial:"shared/upgrade_ie"
#preview.group.sst.ssb.textile(style= "display:none;")
  %h2 Preview
  %h4#preview-title
  #preview-body
.group.sst
  =form_for article,html:{id:"new_article",class:"new-article"} do |f|
    #article-form.group-fields
      %dl
        =render partial:"form",locals:{f:f}
    .group-actions
      %input#submit-article-btn.btn{:name => "commit", :type => "submit", :value => "New"}
      Or
      -if blog.id
        =link_to 'Cancel',blog_path(blog)
      -else
        =link_to 'Cancel',pages_path
:javascript
  App.init = function(){
    new App.Controllers.Articles();
    Backbone.history.start();
  }

  task_name = 'add_content';
