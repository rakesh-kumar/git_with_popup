%tr(id= "subscription-#{s.id}" class='order-subscription')
  %td{colspan: 2}
    %span.note Send e-mail to:
    %strong=subscribtion_title(s)
  -#
    %td.r
      %a
        send test notification
  %td.r{:style => "width:25px"}
    =link_to image_tag('admin/icons/trash.gif'), unfollow_path(s), remote: true, method: :delete, title: 'Remove it', class: :del, data: { confirm: 'Are you sure you want to delete?' }
