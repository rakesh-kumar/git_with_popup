.section
  %h3 By registering a domain name Soqueen
  %p.note
    Soqueen provide a lot of domain names，You can buy。
    = succeed "." do
      =link_to 'Learn how to register a domain name through Soqueen', "#{wiki_url_with_port}/Register_domain"
  / if they have no domains
  .box2.sst
    %p
      %em You have not registered the domain name through Soqueen。By Soqueen registered domain name with your store will automatically bind.
    %form(action="/admin/remote_domains/check" method="post")
      http://www.
      %input#remote_domain_incoming_sld(name="remote_domain[incoming_sld]" size="27" type="text")/
      %select#remote_domain_incoming_tld(name="remote_domain[incoming_tld]")
        %option(value=".com") .com (￥ 88.00 元)
      %input#remote_domain_submit(name="commit" onclick="$('spinner').show(); $('domain-suggestion-container').hide()" type="submit" value="Check new domain name is available")
      %span#spinner(style="display:none")=image_tag 'spinner.gif'
    #domain-suggestion-container.domainavailability.warning(style="display:none;")
      %h4#register-domain-status
      %p#suggestions-title
      %ul#suggestions
    / .domainavailability
/ end section
